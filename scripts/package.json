{
  "name": "@vessel/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "seed-demo-route": "tsx seed-demo-route.ts"
  },
  "dependencies": {
    "@vessel/shared": "workspace:*",
    "uuid": "^11.1.0"
  }
}
